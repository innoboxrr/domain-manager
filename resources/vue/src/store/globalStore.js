import { __domain } from '../utils/translate';
import { defineStore } from 'pinia';

export const useGlobalStore = defineStore('domain-global', {
    state: () => ({
        
    }),
    actions: {
        
    },
});
